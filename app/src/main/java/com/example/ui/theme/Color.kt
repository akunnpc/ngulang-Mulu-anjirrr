package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Obsidian Dark Palette from Immersive UI theme
val ObsidianBg = Color(0xFF0B1014)       // Main background
val SlateCardBg = Color(0xFF161B22)      // Card and surface containers
val ConsoleBg = Color(0xFF050709)        // Darkest tone for live terminal log

val CyberCyan = Color(0xFF22D3EE)        // Glowing cyan accent
val CyberCyanMuted = Color(0x3322D3EE)   // Cyan with low opacity for subtle borders/glow
val EmeraldReady = Color(0xFF10B981)     // Positive, success status, play button
val EmeraldReadyMuted = Color(0x2610B981) // Emerald with low opacity
val WarningGold = Color(0xFFFBBF24)      // Paused / alert status yellow
val WarningGoldMuted = Color(0x26FBBF24)

val BorderSlate = Color(0xFF30363D)      // Slate border gray
val TextLight = Color(0xFFE2E8F0)        // Slate-200 main text
val TextMuted = Color(0xFF8B949E)        // Neutral/gray muted text
val TextDark = Color(0xFF0F172A)         // High contrast dark text for active action buttons
